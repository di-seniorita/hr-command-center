HR-менеджерам каждый день приходится собирать данные из разных мест: таблицы, почта, чаты, рекрутеры. В итоге сложно быстро ответить на простые вопросы: сколько людей на онбординге, кто в зоне риска увольнения, как идёт воронка по кандидатам. HR Command Center — это единый «пульт управления»: один вход, одна система, все ключевые цифры и действия в одном интерфейсе.

Как устроен вход

Сначала HR заходит в систему под своим логином и паролем. После входа открывается личный рабочий стол: дашборд, кандидаты, онбординг, аналитика, отчёты, AI-ассистент, обучение и договоры ГПХ — всё в одном приложении, без переключения между сервисами.

Дашборд

На главном экране сразу видны основные метрики: сколько всего сотрудников, сколько сейчас на онбординге, средняя вовлечённость и процент текучести. Ниже — графики: динамика текучести по месяцам и топ сотрудников с высоким риском ухода. Отдельный блок — алерты: кандидаты, которые давно ждут ответа, сотрудники с высоким риском оттока и просроченные задачи онбординга. Так за пару секунд понятно, куда срочно направить внимание.

Кандидаты и AI-скор

В разделе «Кандидаты» — список по вакансиям с резюме, навыками и статусом (новый, скрининг, интервью, оффер, отказ). У каждого кандидата есть AI-скор — оценка соответствия вакансии по тексту резюме и требованиям (TF-IDF). Можно добавить кандидата вручную или загрузить резюме PDF — система сама извлечёт текст, подставит навыки и пересчитает скор. Статусы можно менять, скор при необходимости пересчитывать по кнопке.

Онбординг и обучение

В «Онбординге» видно сотрудников на адаптации и их задачи: документы, доступы, обучение, знакомство с командой. Задачи можно отмечать выполненными. В «Обучении» — курсы по сотрудникам и их статусы (запланировано, в процессе, завершено). Так видно и процесс входа в компанию, и развитие людей.

Аналитика и риски
В «Аналитике» — сводка по отделам: загрузка, вовлечённость, распределение по департаментам, доля удалённых. Отдельно — риск оттока: модель (логистическая регрессия) учитывает стаж, вовлечённость, удалённость, зарплату и отдел и выдаёт вероятность ухода. Это помогает заранее планировать удержание и разговоры с людьми.

HR-отчёты

Раздел «HR Отчёты» — это готовые визуализации по данным системы: распределение зарплат по отделам, тепловая карта вовлечённости по месяцам, факторы влияния на отток, воронка найма по статусам, сравнение вовлечённости удалённых и офисных. Графики строятся на сервере (matplotlib/seaborn), их можно скачать как PNG и вставить в презентации или отчёты для руководства.

AI-ассистент

В «AI Ассистенте» HR выбирает одного или нескольких кандидатов и задаёт вопрос системе: краткое резюме по кандидату, сравнение по критериям или свой произвольный запрос. Ответ генерирует внешняя языковая модель (LLM): она анализирует резюме и навыки и даёт текстовый вывод. Так можно быстро подготовить выжимку для руководителя или сравнить кандидатов по формулировкам из запроса.

Договоры ГПХ

В разделе «Договоры ГПХ» — список подрядчиков с часами, ставками и статусами (черновик, согласован, подписан). Можно создавать договоры и генерировать акты выполненных работ по шаблону — всё в одном месте с остальными HR-процессами.

HR Command Center объединяет найм, онбординг, аналитику, отчётность и работу с AI в одном приложении. Цель — не заменить живую работу HR, а дать им быстрый доступ к цифрам и подсказкам: кто в риске, кого срочно обработать, как выглядят ключевые метрики и отчёты. Система показывает, как современный HR-инструмент может выглядеть: с авторизацией, дашбордом, AI-скором резюме, прогнозом оттока и AI-ассистентом для работы с кандидатами — всё в одном интерфейсе, без лишних переключений.
Так же к финалу могу сделать модуль сравнения резюме с помощью ИИ с визуальным отображением самих резюме.# HR Command Center

Возможные улучшения для финала: 
1. создание кастомного расширения для браузера, также с логированием и личным входом, которое будет собирать информацию и добавлять работы ИИ модели на странице.
2. На данный момент функция сравнения резюме реализована посредством текстового сравнения с обработкой моделью ИИ на сервере. Возможное добавления визуального сравнения резюме и отображением полученного результата.
3. Отладка и логирование, на данный момент настроены JWT-token авторизации, с настройкой передачи CORS, можно продолжить улучшения как со стороны безопасности так и UI/UX дизайна, путем добавления кнопок навигации и других необходимых кнопок для работы полноценного pipeline приложения. 


HR Command Center — веб-приложение для HR-команды с аналитикой, AI-инструментами и операционными модулями.

## Возможности

- JWT-аутентификация: вход, регистрация, получение профиля пользователя
- Дашборд с KPI, предупреждениями и сводной аналитикой
- Управление вакансиями и кандидатами
- AI-скоринг кандидатов на основе TF-IDF + cosine similarity
- Загрузка резюме в PDF с автоматическим извлечением текста
- **Vision Resume Parser (NEW):** распознавание фото резюме/визитки через внешнюю vision-модель
- Онбординг-задачи сотрудников
- Аналитика и прогноз риска оттока (LogisticRegression)
- HR-отчёты (5 серверных графиков matplotlib/seaborn)
- AI Assistant для HR-запросов к внешней LLM
- Учёт обучения сотрудников
- Договоры ГПХ и генерация акта
- **HR Chatbot (NEW):** плавающая панель помощника с FAQ + fallback в AI

## Стек

### Backend

- Python 3.11+
- FastAPI
- SQLAlchemy
- SQLite
- scikit-learn
- httpx
- python-jose
- passlib
- PyPDF2
- matplotlib
- seaborn
- pandas

### Frontend

- React 18 + Vite
- Tailwind CSS
- Recharts
- Axios
- react-router-dom
- react-hot-toast
- lucide-react

## Структура проекта

```text
/backend   -> FastAPI API, модели БД, сиды, AI-модули
/frontend  -> React приложение (дашборд и страницы HR)
```

## Запуск backend

Откройте терминал в корне проекта и выполните:

```powershell
python -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -r backend/requirements.txt
cd backend && uvicorn main:app --reload --port 8000
```

Backend будет доступен по адресу:

- API: http://localhost:8000
- Swagger: http://localhost:8000/docs

> При первом запуске автоматически создаются таблицы SQLite и заполняются демонстрационными данными на русском языке.

## Запуск frontend

В новом терминале:

```powershell
cd frontend
npm install
npm run dev
```

Frontend будет доступен по адресу:

- http://localhost:5173

## JWT-аутентификация

- В API включена JWT-аутентификация для пользовательских сценариев
- Токен автоматически добавляется frontend-клиентом в Authorization header
- Дефолтный пользователь после сидирования:
  - Логин: `admin`
  - Пароль: `admin`

## LLM интеграция (Qwen2.5-VL)

Приложение интегрировано с внешним LLM-сервисом через `backend/ai_llm.py`.

- Текстовые запросы отправляются на `POST {LLM_BASE_URL}/generate`
- Vision-сценарий использует тот же endpoint с полем `image_base64`
- Базовый URL по умолчанию: `http://localhost:7860`
- Endpoint проверки доступности модели: `GET /api/ai/health`

## Vision Resume Parser (NEW)

Новый функционал распознавания фото резюме/визитки:

- Frontend отправляет `multipart/form-data` (изображение + vacancy_id)
- Backend кодирует изображение в base64 и отправляет в внешнюю vision-модель
- Ответ модели парсится как JSON
- При успешном парсинге автоматически создаётся кандидат с AI-скором
- При неуспехе возвращается сырой ответ модели для ручной проверки

Endpoint:

- `POST /api/ai/parse-resume-image`

## HR Chatbot (NEW)

Новый плавающий чат-виджет для сотрудников:

- Плавающая панель справа на всех защищённых страницах
- Быстрые FAQ-кнопки из базы знаний
- Автоответ из FAQ при совпадении вопроса по ключевым словам
- Если совпадения нет — запрос уходит во внешнюю LLM

Endpoints:

- `POST /api/chatbot`
- `GET /api/chatbot/faq`

## API endpoints

### Auth

- `POST /api/auth/login`
- `POST /api/auth/register`
- `GET /api/auth/me`

### Кандидаты и вакансии

- `GET /api/vacancies`
- `POST /api/vacancies`
- `PUT /api/vacancies/{vacancy_id}`
- `GET /api/candidates`
- `POST /api/candidates`
- `PUT /api/candidates/{candidate_id}`
- `GET /api/candidates/{candidate_id}/rescore`
- `POST /api/candidates/upload-resume`

### AI / LLM

- `GET /api/ai/health`
- `POST /api/ai/summarize`
- `POST /api/ai/compare`
- `POST /api/ai/custom-query`
- `POST /api/ai/parse-resume-image` (NEW)

### Аналитика и алерты

- `GET /api/analytics`
- `GET /api/analytics/churn-risk`
- `GET /api/analytics/turnover-history`
- `GET /api/alerts`

### Онбординг

- `GET /api/onboarding/{employee_id}/tasks`
- `POST /api/onboarding/{employee_id}/tasks`
- `PUT /api/onboarding/{employee_id}/tasks`

### Обучение

- `GET /api/training`

### Договоры ГПХ

- `GET /api/contracts`
- `POST /api/contracts`
- `POST /api/contracts/{contract_id}/generate-act`

### HR отчёты

- `GET /api/reports/salary-boxplot`
- `GET /api/reports/engagement-heatmap`
- `GET /api/reports/churn-factors`
- `GET /api/reports/hiring-funnel`
- `GET /api/reports/remote-vs-office`

### Chatbot

- `POST /api/chatbot` (NEW)
- `GET /api/chatbot/faq` (NEW)

## CORS

Разрешённые origins:

- `http://localhost:5173`
- `http://localhost:3000`
- `http://127.0.0.1:5173`

## База данных

SQLite файл создаётся автоматически в каталоге backend:

- `backend/hr_command_center.db`

Чтобы пересоздать БД с сид-данными:

1. Остановите backend
2. Удалите `backend/hr_command_center.db`
3. Запустите backend заново